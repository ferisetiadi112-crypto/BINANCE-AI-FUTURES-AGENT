# BINANCE AI FUTURES AGENT v0.1 — Environment Configuration
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local or any file containing real credentials.

# ─── Database ──────────────────────────────────────────────────────
# Path to SQLite database file (relative to project root)
DATABASE_PATH=data/agent.db

# ─── Logging ───────────────────────────────────────────────────────
# Log level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# ─── Trading Mode ──────────────────────────────────────────────────
# Paper trading mode — orders are simulated, not sent to exchange
PAPER_TRADING=true

# Trading enabled — set to true only after validation period
TRADING_ENABLED=false

# ─── Risk Boundaries ───────────────────────────────────────────────
# These are testing boundaries, NOT profit guarantees.
INITIAL_CAPITAL=5.00
DAILY_PROFIT_CAP=0.50
DAILY_LOSS_LIMIT=0.50
MAX_LEVERAGE=10
MAX_EXPOSURE_PERCENT=80

# ─── Binance Futures Testnet (Phase 9E) ──────────────────────────
# Testnet keys for Binance Futures Testnet integration.
# All orders execute on https://testnet.binancefuture.com
# NEVER use production keys here.
#
# BINANCE_TESTNET_API_KEY=
# BINANCE_TESTNET_SECRET=
#
# These will be configured through the Keys/API keys UI.
# When PAPER_TRADING=false and testnet keys are set,
# the system places real testnet orders instead of paper trades.

# ─── AI Providers (Phase 9B) ─────────────────────────────────────
# Multi-provider AI integration with automatic fallback routing.
# Fill in at least ONE provider key. Providers are tried in order:
#   1. Groq → 2. Cerebras → 3. Gemini → 4. Mistral → 5. OpenRouter
#
# These will be configured through the Keys/API keys UI.
#
# GROQ_API_KEY=
# CEREBRAS_API_KEY=
# GEMINI_API_KEY=
# MISTRAL_API_KEY=
# OPENROUTER_API_KEY=
